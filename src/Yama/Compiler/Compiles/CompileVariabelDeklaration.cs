using Yama.Index;
using Yama.Parser;

namespace Yama.Compiler
{

    /*public class CompileVariabelDeklaration : ICompile<VariabelDeklaration>
    {
        public string AlgoName
        {
            get;
            set;
        }

        public bool Compile(Compiler compiler, VariabelDeklaration node, string mode = "default")
        {
            //CompileAlgo algo = compiler[this.AlgoName];



            return true;
        }

        //bool Compile ( Compiler compiler, T node );
    }*/

}